# Aegis — konteynerli çalıştırma (Faz 5).
# NOT: Bu yapılandırma geliştirme makinesinde Docker olmadığından canlı test edilmemiştir.
#   docker compose up --build
services:
  db:
    image: postgres:18
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aegis
    ports:
      - "5432:5432"

  soc:
    build:
      context: .
      dockerfile: server/Dockerfile
    environment:
      AEGIS_DATABASE_URL: postgresql+psycopg://postgres:postgres@db:5432/aegis
    ports:
      - "8000:8000"
    depends_on:
      - db

  ml:
    build:
      context: .
      dockerfile: ml/Dockerfile
    ports:
      - "8001:8001"
