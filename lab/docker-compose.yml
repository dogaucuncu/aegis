# Aegis Lab — victim targets (if Docker is available).
# If Docker is not available on this machine, lab/vulnerable_app can be run locally.
#   docker compose -f lab/docker-compose.yml up -d
services:
  juice-shop:
    image: bkimminich/juice-shop
    ports:
      - "3000:3000"

  dvwa:
    image: vulnerables/web-dvwa
    ports:
      - "8081:80"
